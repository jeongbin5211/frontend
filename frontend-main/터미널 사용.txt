// 터미널에서 git bash 실행 
// 터미널 단축키 : ctrl + shift + `(백틱) / MacOS : cmd + shift + `(백틱)

// 터미널의 기본 설정이 git bash로 되어 있지 않은 경우
  >> 터미널 창의 우측 상단에 플러스 모양 우측에 ▽버튼 클릭
  >> '기본 프로필 선택' 클릭
  >> VSCode 상단에 나오는 화면에서 git bash 선택

// 터미널 환경에 외부 코드(문자) 붙여넣기 단축키 : shift + insert

// 터미널 명령어 (리눅스 명령어)

// cd 파일이름 : 현재 디렉터리(폴더)위치에서 해당 디렉터리(하위 폴더)로 이동
// cd .. : 이전 디렉터리(상위 폴더)로 이동